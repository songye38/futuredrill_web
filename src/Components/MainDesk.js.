import React from "react";
import Book from "./Book";
import './../Book.css';
import './../MainDesk.css';

function MainDesk({ books }) {
  return (
    <div className="main-desk">
      {/* 배경 이미지 */}
      <img src='Images/mainDesk.png' alt="Main Desk" className="desk-image" />

      {/* 책 리스트 */}
      <div className="books-container">
        {books.map((book) => (
          <Book key={book.id} book={book} />
        ))}
      </div>
    </div>
  );
}

export default MainDesk;
